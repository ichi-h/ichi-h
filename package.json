{
  "name": "ichi-h",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "spago build --watch --then 'spago run'",
    "build": "spago build",
    "test": "spago test"
  },
  "packageManager": "pnpm@8.15.1",
  "engines": {
    "npm": "use pnpm please!",
    "yarn": "use pnpm please!"
  },
  "dependencies": {
    "purescript": "^0.15.9",
    "spago": "^0.21.0"
  }
}
