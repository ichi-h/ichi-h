{
  "name": "ichi-h",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "spago build --watch --then 'spago run'",
    "build": "spago build",
    "test": "spago test"
  },
  "dependencies": {
    "purescript": "^0.15.9",
    "spago": "^0.21.0"
  }
}
